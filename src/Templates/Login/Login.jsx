import React from 'react'
import ContextProvider from '../../contexts/UserContext'

const Login = () => {
  return (
    <ContextProvider>
      <div></div>
    </ContextProvider>
  )
}

export default Login